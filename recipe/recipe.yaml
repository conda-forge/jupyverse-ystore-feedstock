schema_version: 1

context:
  version: "0.1.2"

package:
  name: jupyverse-ystore
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/j/jupyverse-ystore/jupyverse_ystore-${{ version }}.tar.gz
    sha256: f1d81b43d252d62c16759ad00521c30e56a98cab0a278531ccff12ca8d165d04

build:
  noarch: python
  number: 0
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - uv-build
  run:
    - python >=${{ python_min }}
    - pycrdt

tests:
  - python:
      imports: jupyverse_ystore
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  license: BSD-3-Clause
  license_file: COPYING.md
  homepage: https://github.com/jupyter-server/jupyverse/tree/main/api/ystore
  summary: The API for Jupyverse's YStore.

extra:
  recipe-maintainers:
    - davidbrochart
